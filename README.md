# Betta Tank Tracker

Single-user water parameter log for one Betta aquarium. See `betta-tank-tracker-spec.md` for the full build spec.

## Stack

Vite + React + TypeScript, Tailwind CSS, Supabase, Recharts. Deployed to GitHub Pages, installed as a PWA on iPhone.

## Development

```bash
npm install
npm run dev
```
